<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".activity.MainActivity">

    <android.support.design.widget.TabLayout
            android:id="@+id/tabLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

    <android.support.v4.view.ViewPager
            android:id="@+id/viewPager"
            app:layout_constraintTop_toBottomOf="@+id/tabLayout"
            android:layout_width="match_parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_height="0dp"/>

    <!--<Button android:layout_width="match_parent"
            android:id="@+id/btnSample1"
            android:text="@string/list_with_large_image"
            android:background="@color/colorAccent"
            android:layout_margin="5dp"
            android:textAllCaps="false"
            android:textColor="@color/white"
            app:layout_constraintBottom_toTopOf="@+id/btnSample2"
            android:layout_height="wrap_content"/>

    <Button android:layout_width="match_parent"
            android:layout_margin="5dp"
            android:id="@+id/btnSample2"
            app:layout_constraintTop_toTopOf="parent"
            android:text="@string/list_with_small_image"
            android:background="@color/colorAccent"
            android:textColor="@color/white"
            android:textAllCaps="false"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_height="wrap_content"/>

    <Button android:layout_width="match_parent"
            android:id="@+id/btnSample3"
            android:layout_margin="5dp"
            android:textColor="@color/white"
            android:textAllCaps="false"
            android:text="@string/grid_list_with_large_image"
            android:background="@color/colorAccent"
            app:layout_constraintTop_toBottomOf="@+id/btnSample2"
            android:layout_height="wrap_content"/>-->

</android.support.constraint.ConstraintLayout>